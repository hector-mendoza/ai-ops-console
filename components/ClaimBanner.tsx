export function ClaimBanner() {
  return (
    <header className="rounded-xl border border-emerald-500/30 bg-emerald-950/40 px-6 py-5">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-400">
        Architect · Integration Console
      </p>
      <h1 className="mt-2 text-2xl font-semibold text-zinc-50">
        Natural-language ops become visible integration runs
      </h1>
      <p className="mt-2 max-w-3xl text-sm leading-relaxed text-zinc-400">
        Ask in plain English. The console runs a deterministic venue sync, delivers a
        webhook, updates the live status board, and streams an ops wrap-up — not a chat
        toy.
      </p>
    </header>
  );
}
